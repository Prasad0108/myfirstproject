package com.jwt.authentication.JWTAuthentication.serviceImpl;

import com.jwt.authentication.JWTAuthentication.model.BlackListToken;
import com.jwt.authentication.JWTAuthentication.security.JwtHelper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.core.userdetails.UserDetails;

public class JwtUtil {

//    publi
//
//    public boolean validateToken(String token, UserDetails userDetails) {
//        final String username = jwtHelper.getUsernameFromToken(token);
//        return (username.equals(userDetails.getUsername()) && jwt.isTokenExpired(token) && !tokenBlacklist.isTokenBlacklisted(token));
//    }

}
